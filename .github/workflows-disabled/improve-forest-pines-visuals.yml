name: Improve Forest Pines visuals

on:
  push:
    paths:
      - '.github/workflows/improve-forest-pines-visuals.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Improve Forest Pines canvas assets
        run: python3 .github/scripts/improve_forest_pines_visuals.py
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Improve Forest Pines map visuals"
          git push
