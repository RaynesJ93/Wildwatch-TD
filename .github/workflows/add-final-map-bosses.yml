name: Add final map bosses

on:
  push:
    paths:
      - '.github/workflows/add-final-map-bosses.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Add bosses to Wave 15 of each series final map
        run: python3 .github/scripts/add_final_map_bosses.py
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Add Wave 15 final map bosses"
          git push
