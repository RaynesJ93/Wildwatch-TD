name: Show upgrade currency
on:
  push:
    paths:
      - '.github/workflows/show-upgrade-currency-once.yml'
permissions:
  contents: write
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Update upgrade panel
        run: python3 .github/show-upgrade-currency.py
      - name: Commit update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/show-upgrade-currency-once.yml .github/show-upgrade-currency.py
          git add index.html
          git commit -m "Show current battle currency on upgrade panel"
          git push
